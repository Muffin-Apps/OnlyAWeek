<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Holo">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>
    
    <style name="ListItem">
        <item name="android:paddingTop">@dimen/outter_padding</item>
        <item name="android:paddingBottom">@dimen/outter_padding</item>
        <item name="android:paddingLeft">@dimen/inner_padding</item>
        <item name="android:paddingRight">@dimen/inner_padding</item> 
    </style>
    
    <style name="ValueHeaderText" parent="@android:style/TextAppearance.Small">
        <item name="android:layout_marginLeft">@dimen/inner_padding</item>
        <!-- <item name="android:textColor">#6a6a6a</item-->
    </style>
    
    <style name="ValueText" parent="@android:style/TextAppearance.Medium">
        <item name="android:layout_marginLeft">@dimen/inner_padding</item>
        <!-- <item name="android:textColor">#6a6a6a</item-->
    </style>
    
    <style name="Divider" parent="AppTheme">
        <item name="android:layout_marginLeft">@dimen/inner_padding</item>
        <item name="android:layout_marginRight">@dimen/inner_padding</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:layout_width">fill_parent</item>
        <!-- <item name="android:background">#d6d6d6</item-->
    </style>

</resources>
